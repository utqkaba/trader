import MainCard from "./components/MainCard";

function App() {
  return (
    <div className='flex flex-col h-screen items-center justify-center bg-[conic-gradient(var(--tw-gradient-stops))] from-gray-500 via-gray-900 to-gray-500'>
      <MainCard />
    </div>
  );
}

export default App;